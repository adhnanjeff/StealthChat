{
  "name": "functions",
  "scripts": {
    "build": "tsc",  
    "lint": "eslint . --ext .js",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^11.0.0",
    "firebase-functions": "^4.0.0",
    "bad-words": "^2.0.0"
  },
  "devDependencies": {
    "eslint": "^8.0.0",
    "eslint-plugin-google": "^0.14.0"
  }
}
